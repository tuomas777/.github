[project]
name = "workflow-fixture-django-api-uv"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "django>=5.2,<6",
    "psycopg2-binary>=2.9",
]

[dependency-groups]
dev = [
    "pytest>=8.4",
    "pytest-cov>=6.1",
    "pytest-django>=4.11",
]
